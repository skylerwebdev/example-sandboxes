function writeStory() {
  // grab the element with the id "writeIt"


  // grab the value of the element with the id "person"


  // grab the value of the element with the id "adjective"


  // grab the value of the element with the id "noun"

  /* this is how the story will be written */
  writeIt.innerHTML = `${person} ate a ${adjective} ${noun}... ew!`;
}

// Add an eventlister to the button that adds the function writeStory on click.